# REQ-035: 工具/技能禁用 + 排序下降

> Intake ID: 2026-05-26-tool-disable
> 优先级: 🟡 中

---

## 问题陈述

用户想禁用不需要的工具/技能。被禁用的在列表中排序下降（不删除，只是不展示在靠前位置）。

## 验收标准

| # | AC | 可测试性 |
|---|-----|----------|
| 1 | 工具/技能卡片有"禁用"操作入口 | UI 验证 |
| 2 | 禁用后排序下降到列表底部，显示为灰色 | 视觉验证 |
| 3 | 禁用的工具不传给 LLM（减少 token） | 检查 API 调用 |
| 4 | 可重新启用 | 操作验证 |

## T-Shirt Size

**S (3 pts)** — 前端 UI + config 持久化 + Agent 工具过滤

---
